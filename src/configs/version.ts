/** Configuration constant for app version. */
export const VERSION: string = "0.0.20";
/** Local-storage key used to remember the last version seen by the user. */
export const VERSION_KEY: string = "khqs.version";
