/** Optional rectangle expressed in window or stage coordinates. */
export type WindowRect = {
  /** Optional x coordinate of the top-left corner. */
  x?: number;
  /** Optional y coordinate of the top-left corner. */
  y?: number;
  /** Optional width. */
  width?: number;
  /** Optional height. */
  height?: number;
};
