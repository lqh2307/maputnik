/** Fallback editor viewport width before a rendered container is measurable. */
export const DEFAULT_VIEWPORT_WIDTH: number = 1600;

/** Fallback editor viewport height before a rendered container is measurable. */
export const DEFAULT_VIEWPORT_HEIGHT: number = 1150;
