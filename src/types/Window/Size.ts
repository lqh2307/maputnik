/** Optional box dimensions and edge offsets. */
export type WindowSize = {
  /** Optional left offset. */
  left?: number;
  /** Optional top offset. */
  top?: number;
  /** Optional bottom offset. */
  bottom?: number;
  /** Optional right offset. */
  right?: number;
  /** Optional width. */
  width?: number;
  /** Optional height. */
  height?: number;
};
