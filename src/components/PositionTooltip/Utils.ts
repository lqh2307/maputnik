import { PositionTooltipProp } from "./Types";

/** Performs create reset tooltip info. */
export function createResetTooltipInfo(): PositionTooltipProp {
  return {
    offset: undefined,
    position: undefined,
    children: undefined,
  };
}
